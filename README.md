install all pip or create a virtual env and create it their

Than, Go inside the mainSite directory, that's inside the mainSite directory.
So thats cd mainSite twice

Than say python manage.py runserver
